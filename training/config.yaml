data_dir: "data/processed"
model_dir: "models"
seq_len: 64
batch_size: 32
embed_dim: 128
hidden_dim: 256
num_layers: 2
dropout: 0.2
learning_rate: 0.001
epochs: 30
seed: 42
train_split: 0.85
val_split: 0.15
use_gpu: true
